- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.vim: vim
    ~/.zshrc: zsh/zshrc
- shell:
  - [git submodule update --init --recursive, Installing submodules]

